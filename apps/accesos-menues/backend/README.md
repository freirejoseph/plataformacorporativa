# Backend - Accesos Menues

Backend base del tablero madre.

## Incluye
- API de salud
- lectura de variables desde `.env`
- base preparada para usuarios, roles, grupos y permisos

## Arranque esperado
```bash
uvicorn apps.accesos-menues.backend.main:app --reload
```

## Siguiente paso
Conectar aquí la capa real de SQLite y los CRUD de acceso.
