# Onboarding

## Start Here
1. Open `plataforma.code-workspace` or the root folder.
2. Review `README.md`, `START_HERE.md`, and `ACCESOS_RAPIDOS.md`.
3. Read `SKILL_Plataforma/SKILL.md`.
4. Read the relevant module `SKILL.md` before editing a tab.
5. Use the sync routine when working between local and Ubuntu.

## Current Core Modules
- `accesos-menues`
- `portal-corporativo`
- `administracion-general`

## Development Notes
- Local mirror is a backup.
- Ubuntu at `/home/plataformacorporativa` is the source of truth for execution.
- Keep secrets only in `.env` and never in shared docs.
